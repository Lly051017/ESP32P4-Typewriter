# 步进电机控制模块

本模块为 ESP32-P4 提供高精度步进电机控制功能，支持最多4个独立电机同时运行，适用于写字机项目。

**文档版本**：V2.0  
**适用平台**：ESP32-P4 + Emm42_V5.0 步进电机驱动器  
**更新日期**：2026年5月29日  
**新增功能**：多电机并行控制 API（motor_tasks_init, motor_move_submit, motor_wait_done）

---

## 1. 功能特性

- 支持4个独立电机（X/Y/Z/A轴）
- 位置模式和速度模式控制
- 多电机并行运动控制
- 电机使能/禁用控制
- 原点回归功能
- 状态读取与诊断

---

## 2. 硬件连接

### 2.1 ESP32-P4 引脚分配

| 引脚 | 功能 | 说明 |
|------|------|------|
| GPIO11 | UART2 TX | 发送命令到电机驱动器 |
| GPIO12 | UART2 RX | 接收驱动器状态 |

### 2.2 电机编号

| 编号 | 轴 | 说明 |
|------|------|------|
| 1 | X轴 | 横向移动 |
| 2 | Y轴 | 纵向移动 |
| 3 | Z轴 | 笔尖升降 |
| 4 | A轴 | 备用轴 |

---

## 3. 基础 API

### 3.1 初始化

```c
esp_err_t stepper_motor_init(uart_port_t uart_num, int tx_pin, int rx_pin);
```

**示例**：
```c
stepper_motor_init(UART_NUM_2, GPIO_NUM_11, GPIO_NUM_12);
```

### 3.2 电机使能

```c
esp_err_t motor_enable(uint8_t motor_id, bool enable);
```

**示例**：
```c
motor_enable(MOTOR_ID_X, true);   // 使能X轴
motor_enable(MOTOR_ID_Y, true);   // 使能Y轴
motor_enable(MOTOR_ID_Z, true);   // 使能Z轴
```

### 3.3 位置模式

```c
esp_err_t motor_position_mode(uint8_t motor_id, motor_direction_t dir,
                              uint16_t speed, uint8_t accel,
                              int32_t pulses, position_mode_t mode);
```

**参数解析**：
| 参数 | 类型 | 说明 |
|------|------|------|
| motor_id | uint8_t | 电机编号(1-4) |
| dir | motor_direction_t | 方向(DIRECTION_CW/DIRECTION_CCW) |
| speed | uint16_t | 速度(RPM) |
| accel | uint8_t | 加速度(0-100) |
| pulses | int32_t | 脉冲数 |
| mode | position_mode_t | 模式(POS_MODE_ABSOLUTE/POS_MODE_RELATIVE) |

**示例**：
```c
// X轴顺时针移动1600脉冲，速度200RPM
motor_position_mode(MOTOR_ID_X, DIRECTION_CW, 200, 50, 1600, POS_MODE_RELATIVE);
```

### 3.4 速度模式

```c
esp_err_t motor_speed_mode(uint8_t motor_id, motor_direction_t dir, uint16_t speed);
```

**示例**：
```c
// X轴以300RPM速度连续运行
motor_speed_mode(MOTOR_ID_X, DIRECTION_CCW, 300);
```

### 3.5 停止电机

```c
esp_err_t motor_stop(uint8_t motor_id);
```

**示例**：
```c
motor_stop(MOTOR_ID_X);  // 停止X轴
```

### 3.6 读取状态

```c
esp_err_t motor_read_status(uint8_t motor_id, uint8_t *status);
```

**状态位定义**：
| 位 | 含义 |
|----|------|
| bit0 | 使能状态(1=使能) |
| bit1 | 位置到达(1=到位) |
| bit2 | 运行状态(1=运行中) |

---

## 4. 多电机并行控制 API（新增）

### 4.1 概述

多电机并行控制系统为每个电机创建独立的 FreeRTOS 任务，实现真正意义上的并行运动控制：

- **独立运动**：每个电机有独立的命令队列和状态轮询任务
- **信号隔离**：电机仅响应自身驱动器返回的到位信号
- **并行执行**：X/Y/Z 轴同时启动，不等待其他电机完成
- **完成通知**：使用 EventGroup 实现多电机完成信号同步

### 4.2 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    应用层 (Application)                      │
│  motor_move_submit() → 提交运动命令到各电机队列              │
│  motor_wait_done()   → 等待多个电机同时完成                  │
├─────────────────────────────────────────────────────────────┤
│               并行控制层 (Parallel Control)                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  mt1 (X轴)  │  │  mt2 (Y轴)  │  │  mt3 (Z轴)  │          │
│  │ 命令队列    │  │ 命令队列    │  │ 命令队列    │          │
│  │ 状态轮询    │  │ 状态轮询    │  │ 状态轮询    │          │
│  │ 完成标志    │  │ 完成标志    │  │ 完成标志    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│                    ↓          ↓          ↓                  │
│              s_motor_done_evt (EventGroup)                  │
├─────────────────────────────────────────────────────────────┤
│                    驱动层 (Driver)                           │
│  motor_position_mode() ← s_uart_mutex 保护                  │
│  motor_read_status_fast() ← 快速状态查询                    │
├─────────────────────────────────────────────────────────────┤
│                    硬件层 (Hardware)                         │
│  ESP32-P4 UART2  →  42步进电机驱动器 x 4                    │
│  TX: GPIO11    RX: GPIO12                                   │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 初始化

```c
esp_err_t motor_tasks_init(void);
```

**功能**：创建4个 per-motor FreeRTOS 任务和完成事件组

**调用示例**：
```c
stepper_motor_init(UART_NUM_2, GPIO_NUM_11, GPIO_NUM_12);
motor_tasks_init();  // 必须在 stepper_motor_init 之后调用
```

---

### 4.4 提交运动命令（异步）

```c
esp_err_t motor_move_submit(uint8_t motor_id, motor_direction_t dir,
                            uint16_t speed_rpm, uint8_t accel,
                            int32_t pulses, position_mode_t mode);
```

**功能**：将运动命令提交到指定电机的命令队列（非阻塞）

**参数解析**：与 `motor_position_mode` 相同

**返回值**：
- `ESP_OK`：命令成功入队
- `ESP_FAIL`：队列满，命令被拒绝

**调用示例**：
```c
// 同时提交 X 和 Y 轴运动命令
motor_move_submit(MOTOR_ID_X, DIRECTION_CW, 200, 50, 1600, POS_MODE_RELATIVE);
motor_move_submit(MOTOR_ID_Y, DIRECTION_CW, 200, 50, 1600, POS_MODE_RELATIVE);
// 两个电机立即开始并行运动！
```

---

### 4.5 等待电机完成

```c
esp_err_t motor_wait_done(uint32_t motor_mask, uint32_t timeout_ms);
```

**功能**：等待指定电机组合完成运动（阻塞）

**参数解析**：
| 参数 | 类型 | 说明 |
|------|------|------|
| `motor_mask` | uint32_t | 电机掩码组合 |
| `timeout_ms` | uint32_t | 最大等待时间（毫秒） |

**电机掩码定义**：
```c
#define MOTOR_MASK_X   0x01  // X轴
#define MOTOR_MASK_Y   0x02  // Y轴
#define MOTOR_MASK_Z   0x04  // Z轴
#define MOTOR_MASK_A   0x08  // A轴
```

**返回值**：
- `ESP_OK`：所有指定电机已完成
- `ESP_FAIL`：超时，部分电机未完成

**调用示例**：
```c
// 等待 X 和 Y 轴同时完成
motor_wait_done(MOTOR_MASK_X | MOTOR_MASK_Y, 5000);

// 只等待 Z 轴完成
motor_wait_done(MOTOR_MASK_Z, 3000);

// 等待所有电机完成
motor_wait_done(MOTOR_MASK_X | MOTOR_MASK_Y | MOTOR_MASK_Z, 10000);
```

---

### 4.6 清除完成标志

```c
void motor_clear_done(uint32_t motor_mask);
```

**功能**：清除指定电机的完成标志（用于下一次运动前）

**调用示例**：
```c
motor_clear_done(MOTOR_MASK_X | MOTOR_MASK_Y);
motor_move_submit(MOTOR_ID_X, ...);
motor_move_submit(MOTOR_ID_Y, ...);
motor_wait_done(MOTOR_MASK_X | MOTOR_MASK_Y, 5000);
```

---

### 4.7 查询运动状态

```c
bool motor_is_moving(uint8_t motor_id);
```

**功能**：查询指定电机是否正在运动

**返回值**：
- `true`：电机正在运动
- `false`：电机空闲

**调用示例**：
```c
if (motor_is_moving(MOTOR_ID_X)) {
    // X轴正在运动
}
```

---

### 4.8 完整使用流程

```c
void parallel_move_demo(void)
{
    // 1. 初始化
    stepper_motor_init(UART_NUM_2, GPIO_NUM_11, GPIO_NUM_12);
    motor_tasks_init();
    
    // 2. 使能电机
    motor_enable(MOTOR_ID_X, true);
    motor_enable(MOTOR_ID_Y, true);
    motor_enable(MOTOR_ID_Z, true);
    
    // 3. 清除完成标志
    motor_clear_done(MOTOR_MASK_X | MOTOR_MASK_Y);
    
    // 4. 同时提交 X 和 Y 轴命令
    motor_move_submit(MOTOR_ID_X, DIRECTION_CW, 200, 50, 1600, POS_MODE_RELATIVE);
    motor_move_submit(MOTOR_ID_Y, DIRECTION_CW, 200, 50, 1600, POS_MODE_RELATIVE);
    
    // 5. 等待两个电机都完成
    motor_wait_done(MOTOR_MASK_X | MOTOR_MASK_Y, 5000);
    
    printf("X 和 Y 轴都到达目标位置！\n");
}
```

---

### 4.9 与 GCode 控制器集成

`writer_controller.c` 的 `motor_move_callback` 已自动使用异步 API：

```c
// G1 X30 Y10 F500 执行时：
motor_clear_done(MOTOR_MASK_X | MOTOR_MASK_Y);
motor_move_submit(MOTOR_ID_X, x_dir, speed, accel, pulses_x, POS_MODE_RELATIVE);
motor_move_submit(MOTOR_ID_Y, y_dir, speed, accel, pulses_y, POS_MODE_RELATIVE);
motor_wait_done(MOTOR_MASK_X | MOTOR_MASK_Y, 15000);
```

X 和 Y 轴会**同时启动**，各自独立轮询到位状态，完成时设置 EventGroup 位。

---

## 5. 写字机控制 API

### 5.1 初始化

```c
void plotter_init(uint16_t speed, uint8_t accel);
```

**示例**：
```c
plotter_init(200, 50);  // 速度200RPM，加速度50%
```

### 5.2 抬笔/落笔控制

```c
void plotter_pen_up(void);    // 抬笔
void plotter_pen_down(void);  // 落笔
```

### 5.3 坐标移动

```c
void plotter_move_to(float x, float y, float z);
```

**示例**：
```c
plotter_move_to(100.0, 50.0, 0.0);  // 移动到(100,50,0)
```

### 5.4 图形绘制

#### 5.4.1 绘制直线

```c
void plotter_draw_line(float x1, float y1, float x2, float y2);
```

#### 5.4.2 绘制矩形

```c
void plotter_draw_rect(float x, float y, float width, float height);
```

#### 5.4.3 绘制圆形

```c
void plotter_draw_circle(float cx, float cy, float radius);
```

#### 5.4.4 绘制三角形

```c
void plotter_draw_triangle(float x1, float y1, float x2, float y2, float x3, float y3);
```

### 5.5 字符绘制

```c
void plotter_draw_char(char c, float x, float y, float size);
void plotter_draw_string(const char *str, float x, float y, float size);
```

### 5.6 归位

```c
void plotter_home(void);
```

---

## 6. 使用流程示例

### 6.1 完整绘图流程

```c
void plotter_demo(void)
{
    // 初始化
    stepper_motor_init(UART_NUM_2, GPIO_NUM_11, GPIO_NUM_12);
    motor_tasks_init();
    plotter_init(200, 50);
    
    // 绘制矩形
    plotter_pen_up();
    plotter_move_to(10, 10, 0);
    plotter_pen_down();
    plotter_draw_rect(10, 10, 50, 30);
    
    // 绘制圆形
    plotter_pen_up();
    plotter_move_to(50, 50, 0);
    plotter_pen_down();
    plotter_draw_circle(50, 50, 20);
    
    // 归位
    plotter_pen_up();
    plotter_home();
}
```

### 6.2 自定义绘图示例

```c
void custom_drawing(void)
{
    // 绘制对角线
    plotter_pen_down();
    plotter_move_to(0, 0, 0);
    plotter_move_to(100, 100, 0);
    
    // 绘制三角形
    plotter_draw_triangle(50, 10, 90, 90, 10, 90);
}
```

---

## 7. 配置参数

### 7.1 脉冲-毫米转换系数

```c
#define STEPS_PER_MM_X 80.0f  // X轴每毫米脉冲数
#define STEPS_PER_MM_Y 80.0f  // Y轴每毫米脉冲数
#define STEPS_PER_MM_Z 40.0f  // Z轴每毫米脉冲数
```

### 7.2 笔尖位置

```c
#define PEN_UP_POS    5.0f    // 抬笔位置(mm)
#define PEN_DOWN_POS  0.0f    // 落笔位置(mm)
```

---

## 8. 注意事项

### 8.1 电气安全

- 电机驱动器电源需与 ESP32 隔离
- 使用合适的限流电阻
- 确保接地良好

### 8.2 运动控制

- 速度不宜过高，建议从低速开始测试
- 加速度设置过大可能导致电机失步
- 移动前确保电机已使能

### 8.3 通讯规范

- UART 波特率固定为 115200
- 数据位：8位，无校验，1位停止位
- 命令格式：电机ID + 命令码 + 参数 + 校验和

### 8.4 调试建议

- 使用串口监视器观察状态信息
- 检查电机驱动器指示灯
- 确认接线正确无误

---

## 9. 故障排查

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 电机不转 | 未使能 | 调用 motor_enable() |
| 电机抖动 | 速度过高 | 降低速度参数 |
| 位置不准 | 脉冲数计算错误 | 检查 steps_per_mm |
| 通讯失败 | 接线错误 | 检查 GPIO11/GPIO12 连接 |
| 电机失步 | 加速度过大 | 降低加速度参数 |